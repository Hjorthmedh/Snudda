# First open blender and run the makeNeuronCube.py, adjust the camera,
# then save the blend file. Render it using the command below
# or directly in blender.
blender -b cubewith100neurons.blend -o neuroncube.png -f 1
